export interface SignatureDocument {
  id: string;
  title: string;
  description: string;
  uploadedBy: string;
  uploadedDate: string;
  status: string;
  progress: number;
  signerCount: string;
  signatories: string;
}